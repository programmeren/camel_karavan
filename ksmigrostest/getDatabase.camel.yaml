- route:
    id: route-5a17
    from:
      id: from-ac4a
      uri: timer
      parameters:
        period: "5000"
        timerName: yaml
      steps:
        - to:
            id: to-efb5
            uri: sql
            parameters:
              query: SELECT count(*) FROM core_company?dataSource=#dataSource
        - log:
            id: log-50a6
            message: "Query Result: ${body}"
